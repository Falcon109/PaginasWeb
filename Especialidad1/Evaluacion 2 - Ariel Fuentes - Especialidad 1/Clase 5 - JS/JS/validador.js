const { NOMEM } = require("dns")

function validar(registro){
    //Obtener el valor del registro
    valor = registro.value
    if(value.trim().length=0){
        //No es vacio
        return false
    }
}

function validarEmail (email){
    let validadorEmail = "( /^\w+([.-_+]?\w+)*@\w+([.-]?\w+)/.test(var){
        return false
    }
function cararcteresMax( )

function caracteresNaz(registro){
    valor = registro.value


}

boton = document.getElementById('Enviar').addEventListener('click',validar)

function validar(){
    nombre = document.getElementById('registro_nombre')
}